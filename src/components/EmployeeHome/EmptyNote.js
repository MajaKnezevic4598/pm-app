import React from "react";

import "./EmptyNote.scss";

const EmptyNote = () => {
  return (
    <div className="empty">
      <p>There are no information at the moment.</p>
      <p>Please check back later!</p>
    </div>
  );
};

export default EmptyNote;
